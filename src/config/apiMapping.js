const test = {
    'main': '/test',
    'user': '/user',
}

const file = {
    'main': '/files',
    'upload': '/upload',
    'download': '/download',
}

const apiMapping = {
    'test': test,
    'file': file,
}

module.exports = apiMapping;